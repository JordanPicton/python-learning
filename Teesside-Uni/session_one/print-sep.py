# Example to show off the sep (separation) condition for the print key word.
# Normal Print
print("This is how", "Strings look normally", "when being concatenated.")

# Changing the separation condiotion. Uses "-" characters isntead of the default spaces.
print("This is how", "Strings look when", "changing the sep condition.", "Using the '-' character.", sep="-")